# Develop a program that reads two notes of a student, calculates and displays their average

n1 = float(input('Type the first note: '))
n2 = float(input('Type the second one: '))
print('The average is {:.1f}'.format((n1+n2)/2))
